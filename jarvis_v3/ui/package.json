{
  "name": "jarvis-ui",
  "version": "3.0.0",
  "description": "JARVIS AI Assistant Desktop UI",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "install-all": "npm install && pip install -r ../requirements.txt"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "electron": "^31.0.0"
  }
}
